export type Song = {
  title: string;
  trackNumber: number;
  albumName: string;
  artistName: string;
  diskNumber: number;
  durationMs: number;
  isrc: string;
  appleId: string;
  thumbUrl: string;
};
